# ── Database ──
DATABASE_URL="postgresql://sherlock:sherlock2026@localhost:5432/sherlock_ai?schema=public"

# ── Server ──
PORT=3001
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# ── Auth ──
JWT_SECRET=change-this-to-a-secure-random-string-in-production

# ── AI (optional — the app works without these) ──
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
